# InsForge public client configuration
NEXT_PUBLIC_INSFORGE_URL=
NEXT_PUBLIC_INSFORGE_ANON_KEY=

# AI provider configuration
# "vercel-ai-google" uses the Vercel AI SDK + Google Gemini (recommended for Vercel deployments)
# "gemini" uses the legacy @google/genai SDK directly
AI_PROVIDER=vercel-ai-google
AI_MODEL=gemini-2.5-flash
# Option A: Use Vercel AI Gateway (uses your Vercel credits — get URL from Vercel dashboard → AI tab)
VERCEL_AI_GATEWAY_URL=
# Option B: Use Google API key directly (get from https://aistudio.google.com/apikey)
GOOGLE_GENERATIVE_AI_API_KEY=

# ImageKit public delivery endpoint
NEXT_PUBLIC_IMAGEKIT_URL_ENDPOINT=

# PayPal billing configuration (server-side)
PAYPAL_ENV=sandbox
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_WEBHOOK_ID=
PAYPAL_MONTHLY_PLAN_ID=
PAYPAL_YEARLY_PLAN_ID=
INSFORGE_ADMIN_KEY=

# App URL used for billing redirects
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional InsForge storage bucket overrides
NEXT_PUBLIC_INSFORGE_EXPORTS_BUCKET=theme-exports
NEXT_PUBLIC_INSFORGE_THUMBNAILS_BUCKET=project-thumbnails
